import React from "react";

function index({ title }) {
  return (
    <header className="mb-8">
      <h2 className="text-xl font-medium text-textPrimary">{title}</h2>
    </header>
  );
}

export default index;
